signal=x2;
N=size(signal);
N=N(1);
Nf=8*2^nextpow2(N);
Fs=1;
x = 0:Fs/Nf:(Nf-1)/Nf*Fs;

y = fft(signal, Nf);
y = 1/N*abs(y).^2;

[a, sigma2] =arcov(x,
subplot(1,3,1);
plot(signal);
subplot(1,3,2);
plot(x, y);
set(gca,'xlim',[0,Fs/2]);
%set(gca,'yscale','log')
subplot(1,3,3);
plot(x,yfenetre);
set(gca,'xlim',[0,Fs/2]);
%set(gca,'yscale','log')